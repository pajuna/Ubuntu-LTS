---
- name: install git
  apt: pkg=git state=installed
  become: yes
  tags: git

- name: setup ~/.gitconfig
  become: no
  file:
    src=~/.dotfiles/.gitconfig
    dest=~/.gitconfig
    state=link
    force=yes
  tags: git
